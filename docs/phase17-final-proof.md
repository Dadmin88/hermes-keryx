# Phase 17 definitive final proof

The authenticated relay publisher tests and all previous Phase 17 fixes are committed. This maintainer commit triggers the definitive permanent CI, merge-ref, and authenticated two-node topology gates before cleanup and merge.
