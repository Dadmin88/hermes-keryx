# Phase 17 release candidate

This commit triggers clean-checkout verification of the final Phase 17 source after generated protobufs, authenticated source binding, durable result observation, mapping-shaped artifact compatibility, and relay endpoint configuration have all been committed.

Required green gates:

- permanent Rust CI
- permanent Python SDK CI
- authenticated two-node process topology
- terminal result and artifact return
